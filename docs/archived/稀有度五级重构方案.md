# 稀有度五级重构方案（N / R / SR / SSR / UR）

> 关联文档：`docs/archived/任务大厅与稀有度分级设计方案.md`（旧六级方案，已归档不改写，本文生效后以本文为准）。
> 状态：**已实施完成（2026-08-22）**。门禁全绿：单测 76 例（含新增迁移映射 3 例）、E2E 14 例、tsc / eslint / prettier 通过；旧库迁移经真实冒烟验证（六级存量 → n/r/sr/ssr/ur/ur，二次启动幂等）。

## 一、背景与目标

现行稀有度为六级（common/uncommon/rare/epic/legendary/mythic），中文标签（普通/优秀/稀有/史诗/传说/神话）。实际使用中六级粒度过细、辨识成本高，且顶级两档（传说/神话）几乎无差别。本次重构：

- **六级 → 五级**，命名改为抽卡风格的英文缩写：**N / R / SR / SSR / UR**；
- **展示全部改用英文缩写**，不再显示中文标签；
- 样式五档：**普通样式 / 绿色 / 蓝色 / 紫色 / 彩色（彩虹动画）**；
- 依旧只是标记：标题与稀有度列文字着色、UR 彩虹动画，**不做任何校验与上车拦截**（核心业务规则不变）。

## 二、新旧对照与迁移映射

| 旧值（六级） | 新值（五级） | 显示 | 样式           | 实现                         |
| ------------ | ------------ | ---- | -------------- | ---------------------------- |
| common       | `n`          | N    | 普通（默认色） | 无类名                       |
| uncommon     | `r`          | R    | 绿色           | `text-emerald-600`           |
| rare         | `sr`         | SR   | 蓝色           | `text-sky-600`               |
| epic         | `ssr`        | SSR  | 紫色           | `text-violet-600`            |
| legendary    | `ur`         | UR   | 彩色           | `rarity-ur-text`（彩虹动画） |
| mythic       | `ur`         | UR   | 彩色           | 同上                         |

关键取舍：

1. **存储值用小写** `n/r/sr/ssr/ur`，与库内既有枚举风格一致（status 用 todo/doing/done/carried）；显示层统一大写（`toUpperCase()`），无需再维护中文标签映射。
2. **legendary 与 mythic 合并为 UR**：两个顶档语义重叠，映射时一对一保序优先（common→n … epic→ssr），最高两档归并到 UR。旧六档中的琥珀色（legendary）随之移除。
3. 新增快件的**默认稀有度 = N**（原 common），zod default、`addTask` 兜底、编辑器 initial 三处同步。

## 三、存量数据迁移

与「启动时幂等建表」同一模式：在 `api/ensureSchema.ts` 内追加一组幂等 `UPDATE`，启动时自动完成，重复执行无副作用（旧值迁移后不再存在）：

```sql
UPDATE tasks SET rarity='n'  WHERE rarity='common';
UPDATE tasks SET rarity='r'  WHERE rarity='uncommon';
UPDATE tasks SET rarity='sr' WHERE rarity='rare';
UPDATE tasks SET rarity='ssr' WHERE rarity='epic';
UPDATE tasks SET rarity='ur' WHERE rarity IN ('legendary','mythic');
```

- 迁移映射以常量 `LEGACY_RARITY_TO` 定义在 `db/schema.ts`（新旧值域同处可见），`ensureSchema` 据此生成 UPDATE，配套单测校验映射完整性（旧六级全覆盖、目标值都在新五级内）。
- **`pool_items` 为废弃表，不做数据迁移**：`poolItems.rarity` 的 Drizzle 类型放宽为纯 `text`（去掉 enum 收窄），历史旧值原样保留，反正无人读写。
- 建表 SQL 中 rarity 的 `DEFAULT 'common'` 同步改为 `DEFAULT 'n'`；旧库已加列的 default 残留无影响（Drizzle insert 显式写值，default 不生效）。

## 四、代码触点清单

**后端**

- [x] `db/schema.ts`：`RARITIES` 改为 `["n","r","sr","ssr","ur"]`、`Rarity` 类型随之更新、新增 `LEGACY_RARITY_TO` 映射、`poolItems.rarity` 放宽为 `text`
- [x] `api/ensureSchema.ts`：tasks/pool_items 建表 DEFAULT 改 `'n'`、新增幂等迁移 UPDATE 块
- [x] `api/queries/van.ts`：`addTask` 兜底默认值 `"common"` → `"n"`（`rarityStatsOf` 靠 `RARITIES` 驱动，无字面量不用改）
- [x] `api/vanRouter.ts`：`rarity.default("common")` → `"n"`

**前端**

- [x] `src/pages/BoardPage.tsx`：本地 `Rarity` 类型与 `RARITY_CLASS` 五级化；删除 `RARITY_LABEL` 中文映射，展示统一 `toUpperCase()`
- [x] `src/components/RarityEditor.tsx`：五级列表、面板直接显示大写英文
- [x] `src/components/RarityCellEditor.tsx`：initial 兜底 `"common"` → `"n"`
- [x] `src/index.css`：`.rarity-mythic-text` / `mythic-flow` 改名 `.rarity-ur-text` / `ur-flow`（动画本身保留）

**测试**

- [x] `api/queries/van.test.ts`、`van.unit.test.ts`：`rarityStatsOf` / `toStrandedTask` 用例的枚举值替换（common→n、rare→sr、epic→ssr 等）
- [x] 新增迁移映射完整性单测（`LEGACY_RARITY_TO`：旧值全覆盖、目标∈新五级）
- [x] `api/queries/van.mock.test.ts`：mock 数据中的旧枚举值替换，核对统计断言
- [x] `e2e/bugs.spec.ts`：「稀有度可修改并保存」用例断言 `普通→稀有` 改为 `N→SR`

**文档**

- [x] `README.md`、`AGENTS.md`：「六级稀有度」表述改为五级 N/R/SR/SSR/UR
- [x] `docs/周度发车机制设计方案.md`：仅提「标稀有度」无级别字样，确认后不动
- [x] 旧方案《任务大厅与稀有度分级设计方案.md》已归档，**不改写**（项目规范：归档文档只作历史查阅）
- [x] 实施完成、门禁全绿后，本文移入 `docs/archived/`

## 五、验证与验收标准

1. `npm test` / `npm run check` / `npm run lint` / `npx prettier --check .` 全绿；
2. `npm run test:e2e` 全绿（含稀有度编辑保存用例，显示 N/SR）；
3. **真实迁移冒烟**：临时脚本造一个含全部旧六值的库 → 跑 `ensureSchema` → 断言值变为 `n/r/sr/ssr/ur/ur`，验证后清理；
4. 手动过一遍：新增快件默认 N、编辑器五选一、标题/稀有度列/统计条着色正确、UR 彩虹动画正常。

## 六、明确不做

- 不引入稀有度筛选、排序权重、上车拦截等任何校验逻辑（维持「只是标记」）；
- 不迁移 `pool_items` 废弃表数据；
- 不动版本号（属 v1.0 内迭代）；
- 不改归档文档结论。
